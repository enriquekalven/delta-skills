[project]
name = "delta_skills_alpha_evolve"
version = "0.1.0"
description = "AlphaEvolve optimization experiment for Delta Skills meta-orchestrators"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pytest>=8.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
